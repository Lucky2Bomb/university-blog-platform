export interface IPublication {
    id: string;
    header: string;
    text: string;
    author: string;
    userId: string;
    pictureUrl: string;
    fileUrl: string;
    createdAt: string;
    updatedAt: string;
}