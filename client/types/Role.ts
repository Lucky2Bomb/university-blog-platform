export interface IUserRole {
    id: number,
    roleName: string,
    userId: number,
    createdAt: string,
    updatedAt: string
}

export interface IRole {
    name: string,
    description: string,
    createdAt: string,
    updatedAt: string
}